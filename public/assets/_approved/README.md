# Approved Asset Staging

Use this folder only as a temporary staging area for reviewed/cropped/graded assets before replacing the stable production files.

Production code should keep using the existing stable asset paths documented in `docs/assets/beta-asset-manifest.md`.
